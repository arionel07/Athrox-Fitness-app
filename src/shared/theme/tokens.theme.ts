/**
 * @file shared/theme/tokens.ts
 * @description Токены дизайн-системы FitnessUp.
 *
 * Источник: дизайн-документ v1.0
 * Стиль: Next.js/Vercel минимализм
 *
 * Правило: никаких цветов в компонентах напрямую —
 * только через токены. Это позволяет менять тему
 * в одном месте.
 *
 **/

// ─── Цвета ────────────────────────────────────────────────────────
export const Colors = {
	// ── Основные ────────────────────────────────────────────────────

	background: '#0a0a0a', // фон приложения
	surface: '#141414', // карточки, инпуты
	surfaceHover: '#1a1a1a', // hover состояние карточек
	primary: '#0072f5', // синий акцент — CTA кнопки, активные элементы
	primaryHover: '#0060cc', // darker blue для нажатия

	// ── Текст ───────────────────────────────────────────────────────
	foreground: '#f4f4f5', // основной текст
	muted: '#71717a', // второстепенный текст, placeholder
	subtle: '#3f3f46', // очень тихий текст, разделители

	// ── Границы ─────────────────────────────────────────────────────
	border: '#27272a', // границы карточек
	borderStrong: '#3f3f46', // акцентные границы

	// ── Семантические ───────────────────────────────────────────────
	success: '#17c964', // рост показателей
	warning: '#f5a524', // предупреждения (недостаточно калорий)
	danger: '#f31260', // падение показателей, ошибки
	info: '#0072f5', // информационные метки

	// ── Overlay ─────────────────────────────────────────────────────
	overlay: 'rgba(0,0,0,0.7)' // фон модальных окон
} as const

// ─── Отступы ──────────────────────────────────────────────────────

export const Spacing = {
	xs: 4,
	sm: 8,
	md: 12,
	lg: 16,
	xl: 20,
	xxl: 24,
	xxxl: 32
} as const

// ─── Радиусы ──────────────────────────────────────────────────────

export const Radius = {
	sm: 6,
	md: 10,
	lg: 16, // основной радиус карточек из дизайн-системы
	xl: 20,
	full: 9999
} as const

// ─── Тени ─────────────────────────────────────────────────────────

export const Shadows = {
	card: {
		shadowColor: '#000',
		shadowOffset: { width: 0, height: 2 },
		shadowOpacity: 0.25,
		shadowRadius: 8,
		elevation: 4 // Android
	},
	modal: {
		shadowColor: '#000',
		shadowOffset: { width: 0, height: 8 },
		shadowOpacity: 0.4,
		shadowRadius: 24,
		elevation: 12
	}
} as const

// ─── Размеры иконок ───────────────────────────────────────────────

export const iconSize = {
	sm: 16,
	md: 20,
	lg: 24,
	xl: 32
} as const
